<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Unauthorized asset request — Expo Application Services</title>
    <style type="text/css">
      html {
        --background-color: #fff;
        --link-color: #4630eb;
        --text-color: #25292e;

        background: var(--background-color);
        color: var(--text-color);
        height: 100vh;
      }

      @media (prefers-color-scheme: dark) {
        html {
          --background-color: #14191f;
          --link-color: #a498ed;
          --text-color: #e0e4eb;
          background: var(--background-color);
          color: var(--text-color);
        }
      }

      body {
        font-family: -apple-system, BlinkMacSystemFont, Roboto, Segoe UI, Helvetica Neue, sans-serif;
        font-size: 16px;
        padding: 20px 24px;
      }

      main {
        margin: 0 auto;
        max-width: 640px;
      }

      h1 {
        font-size: 1.5em;
      }

      a {
        color: var(--link-color);
      }
    </style>
  </head>
  <body>
    <main>
      <h1>Unauthorized asset request</h1>
      <p>
        This request for an update asset is missing a valid <code>authorization</code> header.
      </p>
      <p>
        Use a supported client like the <a href="https://docs.expo.dev/versions/latest/sdk/updates/">Expo Updates</a> module to download updates.
      </p>
      <p>
        To access an update asset from a web browser, browse to a specific update from the <a href="https://expo.dev/accounts/[account]/projects/[project]/updates">EAS Update dashboard</a> and view its individual assets.
      </p>
    </main>
  </body>
</html>
